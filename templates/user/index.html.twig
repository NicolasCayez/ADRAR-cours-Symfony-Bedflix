{% extends 'base.html.twig' %}

{% block title %}Sign Up{% endblock %}

{% block body %}
    <div class="form-container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}

    {% if msg is defined %}
        <p>{{ msg }}</p>
    {% endif %}
    </div>
{% endblock %}

